<div class="container">
	<div class="h1 text-center">BUS #99</div>
	<div class="h2 text-center">DVIR</div>
<!-- SIGNATURE AND GENERAL COMMENTS -->

<%= form_tag(wizard_path, method: :put) %>
  <%= hidden_field_tag :signature, class: 'signature_pad_input' %>
  <div class="signature_pad text-center">
    <div class="signature_pad_body">
      <canvas></canvas>
    </div>
    <div class="signature_pad_footer">
      <div class="text-muted">Sign above</div>
      <button type="button" class="btn btn-default signature_pad_clear">Clear</button>
      <%= submit_tag 'Save', class: 'btn btn-success signature_pad_save'  %>
    </div>
  </div>

</div>