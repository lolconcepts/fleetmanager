<%= form_with(model: dvir, local: true) do |form| %>
  <% if dvir.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dvir.errors.count, "error") %> prohibited this dvir from being saved:</h2>

      <ul>
      <% dvir.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.collection_select :company_id, Company.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :vehicle_id %>
    <%= form.collection_select :vehicle_id, Vehicle.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :odometer %>
    <%= form.number_field :odometer %>
  </div>

  <div class="field">
    <%= form.label :end_mileage %>
    <%= form.number_field :end_mileage %>
  </div>

  <div class="field">
    <%= form.label :start_mileage %>
    <%= form.number_field :start_mileage %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
